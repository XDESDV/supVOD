version: '3.8'
services:
  redis:
    container_name: ${APP_NAME}-redis
    image: redis:bullseye
    restart: always
    ports:
      - '${REDIS_PORT}:6379'
    command: redis-server --save 20 1 --loglevel warning
    volumes:
      - redis:/data

volumes:
  redis:
    driver: local